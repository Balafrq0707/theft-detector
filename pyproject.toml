[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "theft-detector"
version = "0.1.0"
description = "Reusable warehouse parcel theft detection library"
readme = "README.md"
requires-python = ">=3.10"

dependencies = []

[tool.setuptools.packages.find]
where = ["src"]